# 🚀 Пошаговая инструкция по деплою на Vercel

## 📋 Что нужно сделать

Я подготовил все файлы для деплоя. Теперь нужно войти в Vercel и настроить проекты.

---

## 🔐 Шаг 1: Вход в Vercel

1. Откройте https://vercel.com/dashboard
2. Войдите через один из способов:
   - **GitHub** (рекомендуется) - самый простой способ
   - Google (nickkipish@gmail.com)
   - Email (nickkipish@gmail.com) - потребуется код подтверждения

**Важно:** После входа откройте ссылку на dashboard в браузере, где уже вошли.

---

## 🔧 Шаг 2: Деплой Backend (FastAPI)

### 2.1 Создание проекта

1. В Vercel Dashboard нажмите **"Add New Project"** или **"New Project"**
2. Если репозиторий еще не подключен:
   - Нажмите **"Import Git Repository"**
   - Выберите GitHub
   - Авторизуйте доступ к репозиторию
   - Найдите репозиторий с проектом Primer
   - Выберите его

### 2.2 Настройка Backend проекта

После выбора репозитория, нажмите **"Configure Project"** и укажите:

- **Framework Preset:** `Other` или `Python`
- **Root Directory:** `backend` ⚠️ (ОБЯЗАТЕЛЬНО!)
- **Build Command:** (оставьте пустым)
- **Output Directory:** (оставьте пустым)
- **Install Command:** `pip install -r requirements.txt`

### 2.3 Переменные окружения Backend

На странице настройки найдите секцию **"Environment Variables"** и добавьте:

| Имя | Значение |
|-----|----------|
| `GEMINI_API_KEY` | `AIzaSyCscw5h8o9ymsSAMHiKgC6frCYHieqAEoY` |
| `ALLOWED_ORIGINS` | `http://localhost:3000` |

⚠️ Позже, после деплоя frontend, нужно будет обновить `ALLOWED_ORIGINS`.

### 2.4 Деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения (обычно 1-3 минуты)
3. После успешного деплоя:
   - Скопируйте **URL проекта** (например: `https://primer-backend-abc123.vercel.app`)
   - Сохраните его - он понадобится для frontend!

---

## 🎨 Шаг 3: Деплой Frontend (Next.js)

### 3.1 Создание проекта

1. Снова нажмите **"Add New Project"**
2. Выберите тот же репозиторий (Primer)

### 3.2 Настройка Frontend проекта

На странице настройки:

- **Framework Preset:** `Next.js` (определится автоматически)
- **Root Directory:** `frontend` ⚠️ (ОБЯЗАТЕЛЬНО!)
- Build и Output определятся автоматически

### 3.3 Переменные окружения Frontend

На странице настройки найдите секцию **"Environment Variables"** и добавьте:

| Имя | Значение |
|-----|----------|
| `NEXT_PUBLIC_API_URL` | `https://primer-backend-abc123.vercel.app` |

⚠️ **Важно:**
- Замените `primer-backend-abc123.vercel.app` на реальный URL из шага 2.4
- URL должен быть **БЕЗ** завершающего слеша (`/`)
- Пример: `https://primer-backend-abc123.vercel.app` ✅
- НЕ: `https://primer-backend-abc123.vercel.app/` ❌

### 3.4 Деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения
3. После успешного деплоя:
   - Скопируйте **URL проекта** (например: `https://primer-frontend-xyz789.vercel.app`)
   - Сохраните его

---

## 🔄 Шаг 4: Обновление CORS в Backend

После деплоя frontend нужно обновить CORS настройки:

1. Вернитесь в **Backend проект** на Vercel
2. Перейдите в **Settings** → **Environment Variables**
3. Найдите `ALLOWED_ORIGINS`
4. Нажмите на значок редактирования (карандаш)
5. Обновите значение:
   ```
   https://primer-frontend-xyz789.vercel.app,http://localhost:3000
   ```
   ⚠️ Замените `primer-frontend-xyz789.vercel.app` на реальный URL из шага 3.4!

6. Нажмите **"Save"**
7. Перейдите на вкладку **"Deployments"**
8. Найдите последний deployment и нажмите **"..."** (три точки)
9. Выберите **"Redeploy"**
10. Подтвердите перезапуск

---

## ✅ Шаг 5: Проверка

### Проверка Backend

1. Откройте в браузере: `https://your-backend-url.vercel.app/health`
2. Должен вернуться JSON:
   ```json
   {
     "status": "healthy",
     "gemini_ready": true,
     "model": "gemini-2.5-flash-image"
   }
   ```

### Проверка Frontend

1. Откройте в браузере: `https://your-frontend-url.vercel.app`
2. Попробуйте загрузить фото и выполнить примерку
3. Откройте консоль браузера (F12) и проверьте на наличие ошибок

---

## 📝 Итоговые переменные окружения

### Backend:
```
GEMINI_API_KEY=AIzaSyCscw5h8o9ymsSAMHiKgC6frCYHieqAEoY
ALLOWED_ORIGINS=https://your-frontend-url.vercel.app,http://localhost:3000
ALLOWED_ORIGIN_REGEX=https://.*\.vercel\.app  (опционально, для preview)
```

### Frontend:
```
NEXT_PUBLIC_API_URL=https://your-backend-url.vercel.app
```

---

## 🐛 Troubleshooting

### Ошибка: "Project not found" при деплое

**Решение:**
- Убедитесь, что репозиторий подключен к Vercel
- Проверьте, что указан правильный Root Directory (`backend` или `frontend`)

### Ошибка: CORS в консоли браузера

**Решение:**
- Проверьте, что `ALLOWED_ORIGINS` в backend содержит точный URL frontend
- Убедитесь, что URL БЕЗ завершающего слеша
- Перезапустите backend (Redeploy)

### Ошибка: 404 на API endpoints

**Решение:**
- Проверьте, что `NEXT_PUBLIC_API_URL` указывает на правильный backend URL
- Проверьте логи backend в Vercel Dashboard

---

## 🎉 Готово!

После выполнения всех шагов ваш проект будет задеплоен и готов к работе!

**Структура:**
- Backend: `https://your-backend-url.vercel.app`
- Frontend: `https://your-frontend-url.vercel.app`

---

**Дата создания:** 2025-11-17

