# 🌐 Настройка MCP Browser Extension для Cursor

## 📋 Что такое MCP Browser Extension?

MCP (Model Context Protocol) Browser Extension позволяет AI ассистенту Cursor использовать браузер для:
- Навигации по веб-страницам
- Взаимодействия с элементами страницы
- Автоматизации задач в браузере
- Работы с веб-приложениями (например, Vercel)

---

## ✅ Проверка: Уже настроено!

MCP Browser Extension уже настроен и доступен в вашем Cursor. Проверить можно по наличию следующих функций:
- `browser_navigate` - навигация по URL
- `browser_click` - клики по элементам
- `browser_type` - ввод текста
- `browser_snapshot` - снимок страницы
- `browser_wait_for` - ожидание элементов

---

## 🔧 Конфигурация MCP

MCP Browser Extension настроен автоматически через расширение Cursor и не требует дополнительной конфигурации в `mcp.json`.

### Текущая конфигурация MCP (`mcp.json`):

```json
{
  "mcpServers": {
    "supabase": { ... },
    "firecrawl-mcp": { ... },
    "perplexity-mcp": { ... },
    "n8n": { ... },
    "context7": { ... },
    "flstudio": { ... }
    // cursor-browser-extension работает автоматически
  }
}
```

**Примечание:** `cursor-browser-extension` не нужно добавлять в `mcp.json` - он работает автоматически через расширение Cursor.

---

## 🚀 Использование

MCP Browser Extension используется автоматически AI ассистентом при необходимости:
- Работа с веб-страницами
- Автоматизация действий в браузере
- Навигация и взаимодействие с элементами

### Примеры использования:

1. **Навигация:**
   ```
   AI автоматически откроет нужный URL
   ```

2. **Взаимодействие:**
   ```
   AI может кликать, вводить текст, заполнять формы
   ```

3. **Снимки страниц:**
   ```
   AI может делать снимки для анализа содержимого
   ```

---

## ⚙️ Разрешения браузера

Для полноценной работы MCP Browser Extension нужны следующие разрешения:

1. **Доступ к активной вкладке** - для взаимодействия с открытыми страницами
2. **Управление вкладками** - для открытия новых страниц
3. **Доступ к данным сайтов** - для чтения содержимого страниц

Эти разрешения обычно предоставляются автоматически при установке расширения Cursor.

---

## 🔍 Проверка работоспособности

MCP Browser Extension работает, если AI ассистент может:
- ✅ Открывать веб-страницы
- ✅ Читать содержимое страниц
- ✅ Взаимодействовать с элементами
- ✅ Делать снимки страниц

---

## 🐛 Troubleshooting

### Проблема: AI не может использовать браузер

**Решение:**
1. Убедитесь, что у вас установлена последняя версия Cursor
2. Проверьте, что расширение Cursor активно
3. Перезапустите Cursor
4. Проверьте, что браузер разрешает доступ расширению

### Проблема: Разрешения не предоставлены

**Решение:**
1. Откройте настройки браузера
2. Перейдите в раздел "Расширения"
3. Найдите Cursor extension
4. Убедитесь, что все разрешения предоставлены

---

## 📚 Дополнительная информация

- [Model Context Protocol Documentation](https://modelcontextprotocol.io/)
- [Cursor Documentation](https://cursor.sh/docs)
- [MCP Browser Extension](https://github.com/modelcontextprotocol/servers)

---

**Статус:** ✅ MCP Browser Extension настроен и готов к использованию!

**Дата:** 2025-11-17

