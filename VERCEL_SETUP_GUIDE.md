# 🚀 Пошаговая инструкция по настройке проекта на Vercel

## 📋 Предварительные требования

1. ✅ Аккаунт на Vercel (если нет - создайте на [vercel.com](https://vercel.com))
2. ✅ GitHub репозиторий с проектом (или GitLab, Bitbucket)
3. ✅ Google Gemini API ключ (если еще нет - получите на [aistudio.google.com/app/apikey](https://aistudio.google.com/app/apikey))

---

## 🎯 Шаг 1: Подготовка репозитория

Убедитесь, что ваш проект находится в Git репозитории:

```bash
# Проверьте статус
git status

# Если нужно закоммитить изменения
git add .
git commit -m "Prepare for Vercel deployment"
git push
```

---

## 🔧 Шаг 2: Деплой Backend на Vercel

### 2.1 Создание проекта Backend

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Нажмите **"Add New Project"** или **"New Project"**
3. Если нужно, подключите ваш Git провайдер (GitHub, GitLab, Bitbucket)
4. Выберите репозиторий с вашим проектом

### 2.2 Настройка Backend проекта

**Важно:** Backend должен быть настроен как отдельный проект!

В настройках проекта укажите:

- **Framework Preset:** `Other`
- **Root Directory:** `backend` ⚠️ (укажите папку backend)
- **Build Command:** (оставьте пустым)
- **Output Directory:** (оставьте пустым)
- **Install Command:** `pip install -r requirements.txt`

### 2.3 Переменные окружения для Backend

На странице настройки проекта найдите секцию **"Environment Variables"** и добавьте:

| Имя переменной | Значение | Описание |
|---------------|----------|----------|
| `GEMINI_API_KEY` | `ваш_ключ_отсюда` | Ваш Google Gemini API ключ (начинается с `AIzaSyC...`) |
| `ALLOWED_ORIGINS` | `http://localhost:3000` | Временно, позже обновим |

⚠️ **Важно:**
- `GEMINI_API_KEY` - получите на [aistudio.google.com/app/apikey](https://aistudio.google.com/app/apikey)
- Значение должно быть БЕЗ кавычек

### 2.4 Деплой Backend

1. Нажмите **"Deploy"**
2. Дождитесь завершения деплоя (обычно 1-3 минуты)
3. После успешного деплоя скопируйте **URL вашего проекта**
   - Например: `https://primer-backend-abc123.vercel.app`
   - Этот URL понадобится для Frontend!

### 2.5 Проверка Backend

Откройте в браузере ваш backend URL:
- `https://your-backend-url.vercel.app/health`
- Должен вернуться JSON с `"status": "healthy"`

---

## 🎨 Шаг 3: Деплой Frontend на Vercel

### 3.1 Создание проекта Frontend

1. В Vercel Dashboard нажмите **"Add New Project"** снова
2. Выберите тот же репозиторий (или создайте отдельный для frontend)
3. Это будет **отдельный проект** от backend

### 3.2 Настройка Frontend проекта

В настройках проекта укажите:

- **Framework Preset:** `Next.js` (определится автоматически)
- **Root Directory:** `frontend` ⚠️ (укажите папку frontend)
- Build и Output настройки определятся автоматически

### 3.3 Переменные окружения для Frontend

На странице настройки проекта найдите секцию **"Environment Variables"** и добавьте:

| Имя переменной | Значение | Описание |
|---------------|----------|----------|
| `NEXT_PUBLIC_API_URL` | `https://your-backend-url.vercel.app` | URL вашего backend проекта (БЕЗ завершающего слеша `/`) |

⚠️ **Важно:**
- Замените `your-backend-url.vercel.app` на реальный URL из шага 2.4
- URL должен быть БЕЗ завершающего слеша (`/`)
- Пример: `https://primer-backend-abc123.vercel.app` ✅
- НЕ: `https://primer-backend-abc123.vercel.app/` ❌

### 3.4 Деплой Frontend

1. Нажмите **"Deploy"**
2. Дождитесь завершения деплоя
3. После успешного деплоя скопируйте **URL вашего frontend проекта**
   - Например: `https://primer-frontend-xyz789.vercel.app`

---

## 🔄 Шаг 4: Обновление CORS в Backend

После деплоя frontend нужно обновить CORS настройки в backend:

### 4.1 Обновите переменные окружения Backend

1. Зайдите в настройки **Backend проекта** на Vercel
2. Перейдите в **Settings → Environment Variables**
3. Обновите `ALLOWED_ORIGINS`:

```
https://your-frontend-url.vercel.app,http://localhost:3000
```

⚠️ **Важно:**
- Замените `your-frontend-url.vercel.app` на реальный URL из шага 3.4
- Разделите URL запятой без пробелов
- URL должны быть БЕЗ завершающего слеша (`/`)

### 4.2 Опционально: Добавьте регулярное выражение

Для автоматического разрешения всех Vercel preview доменов добавьте:

| Имя переменной | Значение |
|---------------|----------|
| `ALLOWED_ORIGIN_REGEX` | `https://.*\.vercel\.app` |

### 4.3 Перезапустите Backend

1. После изменения переменных окружения нажмите **"Redeploy"** в Backend проекте
2. Дождитесь завершения деплоя

---

## ✅ Шаг 5: Проверка работы

1. Откройте ваш Frontend URL в браузере
2. Попробуйте загрузить фото и выполнить примерку
3. Откройте консоль браузера (F12) и проверьте на наличие ошибок
4. Проверьте логи backend в Vercel Dashboard:
   - Зайдите в Backend проект
   - Откройте вкладку **"Logs"**

---

## 🐛 Troubleshooting

### Проблема: CORS ошибки

**Симптомы:**
- В консоли браузера: `Access to fetch at ... from origin ... has been blocked by CORS policy`

**Решение:**
1. Проверьте, что `ALLOWED_ORIGINS` в backend содержит точный URL frontend
2. Убедитесь, что URL начинается с `https://` (не `http://`)
3. Проверьте, что URL БЕЗ завершающего слеша
4. Перезапустите backend после изменения переменных

### Проблема: 404 на API endpoints

**Симптомы:**
- В консоли: `404 Not Found` при вызове API

**Решение:**
1. Проверьте, что `NEXT_PUBLIC_API_URL` в frontend указывает на правильный backend URL
2. Убедитесь, что backend успешно задеплоен (проверьте логи)
3. Проверьте, что URL БЕЗ завершающего слеша

### Проблема: Timeout ошибки

**Симптомы:**
- Запросы завершаются с ошибкой timeout

**Решение:**
- Vercel Hobby план имеет лимит 10 секунд на выполнение функции
- Для длительных операций рассмотрите Vercel Pro план
- Или используйте внешний сервис для backend (Railway, Render, Fly.io)

### Проблема: Gemini API ошибки

**Симптомы:**
- В логах backend: ошибки Gemini API

**Решение:**
1. Проверьте, что `GEMINI_API_KEY` правильно установлен в backend
2. Убедитесь, что ключ валидный и не истек
3. Проверьте квоты API в [Google AI Studio](https://aistudio.google.com/app/apikey)

---

## 📝 Итоговая структура переменных окружения

### Backend (Vercel Environment Variables):
```
GEMINI_API_KEY=AIzaSyC...
ALLOWED_ORIGINS=https://your-frontend-url.vercel.app,http://localhost:3000
ALLOWED_ORIGIN_REGEX=https://.*\.vercel\.app  (опционально)
```

### Frontend (Vercel Environment Variables):
```
NEXT_PUBLIC_API_URL=https://your-backend-url.vercel.app
```

---

## 🔗 Полезные ссылки

- [Vercel Dashboard](https://vercel.com/dashboard)
- [Vercel Documentation](https://vercel.com/docs)
- [Vercel Python Runtime](https://vercel.com/docs/runtimes/python)
- [Next.js Environment Variables](https://nextjs.org/docs/basic-features/environment-variables)
- [Google AI Studio](https://aistudio.google.com/app/apikey)

---

## ✨ Готово!

После выполнения всех шагов ваш проект должен работать на Vercel! 🎉

**Структура деплоя:**
- **Backend:** `https://your-backend-url.vercel.app`
- **Frontend:** `https://your-frontend-url.vercel.app`

---

**Создано:** $(Get-Date -Format "yyyy-MM-dd HH:mm:ss")

